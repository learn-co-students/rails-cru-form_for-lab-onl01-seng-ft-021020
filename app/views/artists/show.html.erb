<h1><%= @artist.name %></h1>
<p><%= @artist.bio %></p>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.name, song %> - <%= link_to song.genre.name, song.genre %> 
  <% end %>
</ul>
